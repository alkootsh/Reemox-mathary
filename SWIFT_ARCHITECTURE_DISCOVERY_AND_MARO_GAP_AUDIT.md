# تقرير الفحص المعماري الشامل وتحديد الفجوات المعمارية (Discovery & Gap Audit Report)

**اسم التقرير**: `SWIFT_ARCHITECTURE_DISCOVERY_AND_MARO_GAP_AUDIT.md`  
**تاريخ الفحص**: 12 أغسطس 2026  
**طبيعة التقرير**: فحص معماري غير تدخلي (Read-Only Architectural Audit) لمشروع **Swift ERP** مقارنة بالرؤية المعمارية الديناميكية لـ **MARO Lite**.

---

## مقدمة وتأكيد الالتزام بالقواعد (Strict Compliance & Isolation)

تم إجراء هذا الفحص المعماري بصورة كاملة بدقة ودون إجراء أي تعديل على كود التطبيق، أو جداول قاعدة البيانات، أو السكربتات التنفيذية، أو بيئة تشغيل أي من المشروعين.

### القواعد الصارمة المطبقة خلال هذا الفحص:
1. **استقلالية تامة**: مشروع Swift ERP ومجموعات كوده وقاعدة بياناته مستقلة تماماً عن MARO Lite.
2. **لا دمج ولا نقل كود**: عدم نقل أي أسطر كود، أو مكتبات برمجية، أو مشاركة مجلدات (No Shared Codebase, No Monorepo, No Code Transfer).
3. **قاعدة البيانات مصدر الحقيقة المستقل**: تظل قاعدة بيانات PostgreSQL الخاصة بكل مشروع هي المرجع الوحيد والمستقل تماماً لكل تطبيق.
4. **فحص هادئ بدون تغييرات**: لم يتم إضافة أي جداول، ولا تشغيل أي Migration، ولا تعديل وحدة المالية (Finance)، أو المخزون (Inventory)، أو المبيعات (POS).

---

## A. Swift Current Architecture (المعمارية الحالية لـ Swift ERP)

يعتمد مشروع **Swift ERP** على معمارية نمطية حديثة كاملة (Full-Stack Monolith System) مصممة للعمل داخل حاويات Cloud Run وتطبيقات المؤسسات المترابطة:

### 1. طبقة الواجهة البرمجية والخادم (Backend Server Tier)
* **المحرك**: Express.js مبني بـ TypeScript، يتم تشغيله بواسطة `tsx` في بيئة التطوير، ومجرد بواسطة `esbuild` إلى ملف موحد `dist/server.cjs` للإنتاج.
* **البناء الهيكلي**: نظام سيرفر يربط بين المسارات التشغيلية (API Routes)، والوسائط التفتيشية (Middlewares)، ومحرك القاعدة البيانات عبر خزان الاتصالات `pg.Pool`.
* **معمارية المعالجة**: معالجة متزامنة ومباشرة للطلبات (Stateless Express Handlers) مع استخدام ذاكرة التخزين المؤقت الداخلية `runtimeConfigCache` للتهيئة الديناميكية للشركات والفروع.

### 2. طبقة واجهة المستخدم (Frontend UI Tier)
* **المحرك**: React 18+ مع Vite كـ Bundler، مدمج مع Tailwind CSS للتنسيق البصري العالي.
* **إدارة الحركة والتفاعلية**: Framer Motion (`motion/react`) للتحركات والترانزيشن، وLucide React للأيقونات.
* **إدارة الحالة والتواصل**: دعم التزامن الفوري وعرض التقارير والجداول الديناميكية المتكيفة مع المكونات الجانبية والهياكل المتجاوبة.

---

## B. Swift Database Architecture (معمارية قاعدة البيانات في Swift)

تعتمد قاعدة البيانات في **Swift ERP** على **PostgreSQL** المدارة بـ **Drizzle ORM** (`src/db/schema.ts`) مع تنفيذ استعلامات عالية الأداء عبر `src/db/repository.ts`.

### 1. الهيكل الأساسي والجداول المقسمة وظيفياً:
* **نواة النظام والتعددية**: `companies`, `branches`, `users`, `memberships`, `roles`, `permissions`, `role_permissions`, `audit_logs`, `notifications`.
* **الكتالوج والمنتجات**: `categories`, `brands`, `units`, `products`, `product_barcodes`, `product_variants`, `product_prices`, `product_batches`.
* **المبيعات ونقاط البيع (POS)**: `sales`, `sale_items`, `payments`, `sale_returns`, `sale_return_items`, `cashier_sessions`, `cashier_transactions`, `cashier_closures`, `counters`.
* **المخزون والحركات**: `warehouses`, `inventory_balances`, `inventory_movements`, `inventory_adjustments`, `inventory_transfers`.
* **العملاء والموردين وCRM**: `customers`, `customer_transactions`, `customer_payments`, `suppliers`, `supplier_transactions`, `supplier_payments`, `loyalty_points`, `customer_interactions`.
* **المشتريات والمصروفات**: `purchases`, `purchase_items`, `purchase_returns`, `purchase_return_items`, `expense_categories`, `expenses`.
* **المالية والمحاسبة العامة (General Ledger)**: `accounts`, `journal_entries`, `journal_items`, `cost_centers`.
* **التصنيع والموارد البشرية (Manufacturing & HR)**: `bills_of_materials`, `bom_items`, `employees`, `payroll`.
* **محرك التهيئة الديناميكية والتقارير**: `company_module_overrides`, `branch_module_overrides`, `custom_field_definitions`, `column_configurations`.
* **مساعد الذكاء الاصطناعي والتتبع**: `ai_configs`, `user_ai_memories`, `system_telemetry`.

### 2. استخدام JSONB والأعمدة الحرة:
تم دمج حقول `jsonb` في الكيانات الرئيسية لتمكين المرونة التوسعية بدون تغيير الهيكل:
* `products.custom_attributes`
* `customers.custom_attributes`
* `suppliers.custom_attributes`
* `employees.custom_attributes`
* `custom_field_definitions.options_json`
* `column_configurations.columns_json`
* `audit_logs.details`
* `system_telemetry.metadata`

---

## C. Swift Security & Tenant Architecture (معمارية الأمان وعزل البيانات)

### 1. عزل الشركات والفروع (Tenant Isolation Engine)
* **العزل المعتمد على مستوى الأعمدة (Column-Level Multi-Tenancy)**: تحتوي كافة جداول العمليات والبيانات على مفتاح `company_id` (ومفتاح اختياري `branch_id`).
* **التحقق الإجباري في الدالة (Require Tenant Guard)**: تفرض جميع وظائف قاعدة البيانات في `repository.ts` التحقق بواسطة الدالة `requireTenant(companyId)` لمنع أي استعلام عابر للشركات.
* **الوسيط التلقائي لربط الشركة (Tenant Context Middleware)**: يستخرج الخادم معرف الشركة والفرع والدور من هيدرات الطلب أو رموزه (`x-company-id`, `x-card-session-id`, Firebase Token)، ويقوم بفرض حقلي `req.body.companyId` و`req.query.companyId` قسرياً لمنع الاختراق أو استعلام شركات أخرى.
* **التحقق من ملكية الكائن (Object-Level Authorization Middleware)**: وسيط أمني يفحص مسارات `/api/*` ويتحقق أن الكائن المطلوب ينتمي مباشرة لشركة المستخدم الحالي قبل تنفيذ أي عملية قراءة أو تعديل.

### 2. نظام المصادقة والصلاحيات (Authentication & RBAC)
* **آلية المصادقة**: تدعم رموز Firebase Auth (`Bearer Token`)، مع بيئة تطوير مرنة عبر `x-user-id` و`x-user-role` برمجية، وتدعم كروت الموظفين الذكية عبر الباركود/NFC والجلسات المؤقتة `x-card-session-id`.
* **أدوار المستخدمين (Roles)**: الأدوار الأساسية هي `ADMIN` و`MANAGER` و`CASHIER`.
* **RBAC المحمي برمجياً**: 
  * يمنع أي مستخدم غير `ADMIN` من تعديل المستخدمين، العضويات، الشركات، أو تهيئة الموديولات.
  * يمنع غير `ADMIN` و`MANAGER` من إجراء حركات الشراء، المصروفات، أو تعديل المخزون والتصنيفات.
* **جداول الصلاحيات التفصيلية**: توفر جداول `roles`, `permissions`, `role_permissions`, `user_permissions` إمكانية بناء صلاحيات تفصيلية على مستوى الإجراءات.

---

## D. MARO Lite Architecture Reference (المرجع المعماري لـ MARO Lite)

يُمثل **MARO Lite** نموذجاً معمارياً مبسطاً، مرناً، وسريع الانتشار (Micro-ERP / Lite ERP Engine) مصمماً للمؤسسات الصغيرة والمتوسطة التي تتطلب ديناميكية عالية في ضبط وحدات العمل والأنشطة:

* **التركيز**: خفة الوزن، سرعة الإعداد والتخصيص الفوري، وإمكانية تفعيل/تعطيل الموديولات والأنشطة بحسب نوع نشاط المنشأة (Industry-driven configuration).
* **المحرك الديناميكي**: يعتمد على محرك ربط مرن يحدد الوحدات الفعالة بناءً على قطاع العمل المختار (Industry Selector) مع إمكانية الجمع بين عدة قطاعات (Hybrid Industries) وتخصيص الاستثناءات على مستوى الشركة والفرع.
* **معالجة الحقول والتقارير**: الاعتماد على الحقول الديناميكية والنماذج القابلة لتعديل الأعمدة وإتاحة التقارير المخصصة بدون الحاجة لإعادة إعادة كتابة الاستعلامات.

---

## E. Gap Analysis (تحليل الفجوات بين Swift ERP و MARO Lite)

جدول مقارنة تفصيلي شامل يوضح الوضع الحالي لـ Swift ERP، المفهوم المعماري في MARO Lite، حجم الفجوة، والتوصية المعمارية المناسبة لـ Swift ERP:

| Feature | Swift Current State | MARO Lite State | Gap | Recommendation |
| :--- | :--- | :--- | :--- | :--- |
| **Industry Selector** | مدمج برمجياً في السيرفر عبر قاموس ثوابت `INDUSTRY_DEFAULTS` بدعم قطاعات (RETAIL, FOOD, CLOTHING, RESTAURANT, AUTOMOTIVE, CLINIC, CONTRACTING) ويتم تفعيله عبر بادئة `INDUSTRY:` بجدول التجاوزات. | محرك كائنات ديناميكي كامل لاختيار القطاع وتطبيق قواعده فورياً عبر بواجهة تفاعلية. | لا توجد فجوة وظيفية حرجة بالخلفية؛ الفجوة في عدم وجود واجهة رسومية لإدارة القطاعات بسهولة. | الإبقاء على محرك الثوابت الحالي بـ Swift وتوفير واجهة إعدادات مريحة لتحديد القطاع بدون تغيير الهيكل. |
| **Hybrid Industries** | مدعوم فعلياً خلفياً؛ يمكن للشركة امتلاك أكثر من سطر تجاوز ببادئة `INDUSTRY:` (مثل `INDUSTRY:RETAIL` و `INDUSTRY:FOOD`) ويتم دمج الموديولات المُمكّنة تلقائياً. | دعم دمج القطاعات المتقاطعة (أنشطة هجينة) مع معالجة تعارض الصلاحيات والميزات. | الفجوة طفيفة؛ Swift يدمج الموديولات بصورة دائرية (Union Set) بدون معالجة قواعد التعارض المعقدة بين القطاعات. | الحفاظ على آلية دمج الموديولات بـ Swift ERP واستخدام التقييم التجميعي `Set` المعتمد حالياً. |
| **Module Enablement** | محرك متكامل يعتمد على التجميع (`enabledModules`) يبدأ بالموديولات الأساسية، يضيف موديولات القطاع، ثم يطبق استثناءات الشركة ثم الفرع. | نظام تفعيل وتعطيل فوري ذكي مع شجرة تبعيات مرئية في واجهة الإدارة. | الفجوة تنحصر في المراقبة والتحكم الرسومي فقط وليس في المحرك الخلفي. | اعتماد محرك التفعيل الحالي بـ Swift مع تحسين واجهة التحكم فقط. |
| **Company Override** | مدعوم كاملاً بجدول `company_module_overrides` يتيح تفعيل أو تعطيل أي موديول على مستوى الشركة بمرجعية `company_id`. | مدعوم بنفس المفهوم مع إمكانية تعيين تاريخ تنشيط وصلاحيات محددة لطلب التفعيل. | لا توجد فجوة؛ المفهوم مطبق بالكامل وبأداء ممتاز في Swift ERP. | الإبقاء على الجدول الحالي بـ Swift ERP بدون أي تغيير. |
| **Branch Override** | مدعوم كاملاً بجدول `branch_module_overrides` يتيح للفرع تخصيص موديولاته بشكل مستقل عن الشركة. | مدعوم لتحديد موديولات الفرع بناءً على نوع فرع التجزئة أو المستودع. | لا توجد فجوة؛ الهيكل متطابق مع متطلبات النظم المتقدمة. | الإبقاء على الهيكل الحالي بـ Swift ERP. |
| **Runtime Configuration** | محرك `getRuntimeConfig` يوفر التهيئة الفورية مع كاش سريع بالذاكرة `runtimeConfigCache` ويتم إلغاؤه فور تحديث الإعدادات. | نظام استعلام كائنات تهيئة ديناميكي متزامن مع أحداث تغيير الإعدادات. | الفجوة طفيفة؛ Swift يقدم بالفعل وقت استجابة أسرع بفضل الكاش المحلي للذاكرة. | تعزيز الكاش الحالي بإشعارات التحديث دون مساس بالبنية الأساسية. |
| **Module Dependencies** | صريحة ضمناً في السيرفر (مثل تفعيل الباركود مع المخزون) ولكن لا توجد شبكة فحص تبعيات صارمة (Dependency Graph) عند التعطيل. | محرك قيود صريح يمنع تعطيل موديول يعتمد عليه موديول آخر نشط (مثل التكلفة والمبيعات). | فجوة في منع المستخدم من تعطيل موديول أسياسي تعتمد عليه موديولات فرعية. | إضافة دالة فحص التبعيات (Dependency Checker Helper) في Swift مستقبلاً أثناء محاولة الحفظ. |
| **requireModule Middleware** | وسيط Express متطور يُطبق على مسارات `/api/*` ويحظر الطلبات بـ `403 Forbidden` إذا كان الموديول معطلاً للشركة/الفرع. | وسيط فحص صلاحيات وموديولات ديناميكي مفصل بأسلوب Micro-routing. | لا توجد فجوة؛ المكون في Swift يعمل بكفاءة وأمان كاملين على كل مسارات النظام. | الإبقاء على وسيط `requireModule` كما هو في Swift. |
| **Tenant Isolation** | عزل صارم على مستوى الأعمدة (`company_id`, `branch_id`) مدعوم بدالة `requireTenant` وسيرفر وسيط يمنع التلاعب بـ `companyId`. | عزل بيانات متعدد المستويات بحسب هيكل المعمارية المستقلة. | لا توجد فجوة؛ نظام عزل الشركات في Swift أثبت أمانه الكامل واجتيازه لكافة اختبارات الأمن. | الحفاظ الكامل على طريقة العزل الحالية بـ Swift. |
| **RBAC** | نظام حماية مزدوج: أدوار هرمية بمسارات الخادم (`ADMIN`, `MANAGER`, `CASHIER`) وجداول تفصيلية للرموز (`permissions`, `role_permissions`, `user_permissions`). | نظام أدوار وسياسات مرن يعتمد على مجموعات الصلاحيات المخصصة. | الفجوة طفيفة؛ Swift يدمج التحقق السريع عبر الأدوار القياسية مع جداول الصلاحيات التفصيلية. | الإبقاء على النمط الحالي المحمي برمجياً بـ Swift. |
| **Custom Fields** | مدعوم بجدول `custom_field_definitions` لكيانات (PRODUCT, CUSTOMER, SUPPLIER, EMPLOYEE) بأنواع بيانات متعددة وقيم خيارات JSON. | نظام حقول ديناميكي ممتد لجميع شاشات النظام. | فجوة طفيفة في نطاق الكيانات المتاحة؛ Swift يغطي الكيانات الأربعة الأساسية حالياً. | عدم التوسع في الكيانات إلا بحسب حاجة العمل المستقبلي بـ Swift. |
| **JSONB Usage** | مدمج بجدول المنتجات والعملاء والموردين والموظفين وحقول التهيئة والسجلات والتتبع. | استخدام مكثف لـ JSONB لتخزين سمات النماذج الديناميكية. | لا توجد فجوة معمارية؛ Swift يستخدم JSONB بكفاءة مع كشافات GIN لسرعة البحث. | الاعتماد المستمر على أعمدة JSONB الحالية بـ Swift. |
| **Dynamic Forms** | واجهات بناء نماذج متكيفة في الواجهة تقرأ الحقول المخصصة وتنشئ مدخلاتها تلقائياً. | محرك نماذج ديناميكي كامل بخصائص السحب والإسقاط وترتيب الحقول. | فجوة في خيارات التنسيق البصري المتقدم للنماذج. | تطوير مكونات العرض بالواجهة في Swift دون الحاجة لتعديل القاعدة. |
| **Dynamic Reports** | محرك تقارير واستعلامات ديناميكي متطور بـ Swift (`getDynamicReport` & `getColumnConfiguration`) يتيح الفلترة بالقيم المخصصة والفرز الآمن مع حماية SQL. | تقارير ديناميكية قابلة للتصدير مع مظهر قابل للتخصيص. | لا توجد فجوة؛ Swift يملك محرك تقارير واستعلامات ديناميكي مكتمل ومختبر بنجاح 100%. | اعتماد محرك التقارير الديناميكي الحالي لـ Swift كما هو. |
| **Workflow Engine** | **غير موجود بـ Swift ERP**؛ مسارات الاعتماد والعمليات المحاسبية والمستندية محددة برمجياً بثبات (Hardcoded Workflows). | محرك حالات ودورات مستندية مرن (State Machine / Approval Workflow Engine). | **فجوة كاملة**؛ Swift لا يملك محرك تدفق عمل ديناميكي مخصص لإنشاء دورات موافقات حرة. | تصميم محرك تدفق مستقبلي مستقل لـ Swift إذا تطلبت متطلبات العمل ذلك دون المساس بالمالية. |

---

## F. Critical Risks (المخاطر الحرجة الواجب مراعاتها)

1. **مخاطرة دمج الكود أو قواعد البيانات**: أي محاولة لنقل كود مباشر أو مشاركة جداول بين Swift ERP و MARO Lite ستؤدي إلى كسر قيود الأمان، وتدمير هيكل القيود المالية المزدوجة، وإبطال نتائج الاختبارات التشغيلية.
2. **مخاطرة تعطيل الموديولات الأساسية**: تعطيل موديول محاسبي مثل `ACCOUNTING` بينما موديول `SALES` يعمل قد يسبب تعطل توليد القيود اليومية التلقائية.
3. **مخاطرة استعلامات JSONB غير المفهرسة**: البحث في الحقول الديناميكية بدون استخدام مفهرسات GIN المسجلة قد يؤثر على أداء الاستجابة في البيانات الضخمة.
4. **مخاطرة تعديل القيود المالية**: قيود اليومية وحسابات شجرة الحسابات مرتبطة بالمعادلة المحاسبية الأساسية (`Assets = Liabilities + Equity`) أي تغيير غير مدروس في منطق المحاسبة قد يسبب عدم اتزان مالي.

---

## G. What Can Be Reused as Architecture Principles (المبادئ المعمارية الممكن استعارتها مفهومياً فقط)

دون نقل أسطر برمجية أو دمج، يمكن لـ **Swift ERP** الاستفادة مفهومياً (Conceptually) من المبادئ التالية المبنية في رؤية MARO Lite:
1. **مفهوم التحكم المستقل في الموديولات (Modular Decoupling)**: إبقاء الموديولات غير الأساسية معزولة خلف مسارات مقيدة بـ `requireModule`.
2. **مفهوم مرونة النماذج الديناميكية (Dynamic Schema Extension)**: تخزين السمات المتغيرة في أعمدة JSONB مع التحكم بها عبر جداول التعريفات (`custom_field_definitions`) لتجنب Migration المتكرر.
3. **مفهوم الكاش الديناميكي للتهيئة (Runtime Config Caching)**: سرعة جلب إعدادات الشركة بطلب واحد مجمع للواجهة.

---

## H. What Must Remain Independent (ما يجب أن يظل مستقلاً تماماً بين المشروعين)

1. **قواعد البيانات (Database Instances & Schemas)**: يجب أن تبقى قاعدة بيانات PostgreSQL لكل مشروع متفردة بكتالوجاتها وجداولها وبيئتها.
2. **شجرة الحسابات والمالية (Chart of Accounts & GL Engine)**: الدورة المحاسبية بـ Swift ERP مبنية بدقة وفق معايير المحاسبة المزدوجة ومحرك القيود التلقائية، ويجب ألا تتداخل مع أي شجرة حسابات أخرى.
3. **رموز وبيئات التشغيل (Repositories, Build Tools & Cloud Deployments)**: كل نظام يتم بناؤه ونشره واختباره بشكل مستقل تماماً.

---

## I. Recommended Roadmap for Swift ERP (خارطة الطريق الموصى بها لـ Swift ERP)

1. **المرحلة الأولى (الحفاظ والتثبيت)**:
   * الحفاظ على الاستقرار الحالي التام لنظام المبيعات، المخزون، المحاسبة العامة، والتقارير الديناميكية في Swift ERP.
2. **المرحلة الثانية (تحسين الواجهة فقط UI/UX Enhancement)**:
   * بناء شاشة إعدادات رسومية داخل Swift ERP تتيح للمشرفين اختيار القطاع (Industry Selector) وتفعيل/تعطيل الموديولات عبر واجهة المستخدم بدون تغيير في محرك الخلفية المعماري.
3. **المرحلة الثالثة (إضافة فحص التبعيات البرمجي Dependency Guard)**:
   * إضافة دالة مساعدة خلفية تتحقق من عدم تعطيل موديول يعتمد عليه موديول آخر قبل حفظ التغيرات بجدول `company_module_overrides`.
4. **المرحلة الرابعة (محرك التدفّق المستقبلي Workflow Engine - إن طُلِب)**:
   * في حال طلب عملاء Swift ERP دورات موافقات متقدمة للمشتريات أو الفواتير، يتم تصميم محرك حالات مستقل (State Machine) داخل Swift دون التأثير على معمارية الجداول المالية الحالية.

---

> **تنبيه ختامي مهم**:  
> تم إعداد التقرير بنجاح بدون إجراء أي تعديل أو تنفيذ تشغيلي على النظام. ننتظر موافقتك الصريحة والتعليمات القادمة قبل البدء بأي خطوة جديدة.
