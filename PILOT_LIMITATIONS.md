# قيود وتحديدات مرحلة التشغيل التجريبي (PILOT_LIMITATIONS.md)
MARO Lite ERP - Pilot Version

---

## 1. قيود أمان واجهة المستخدم (Client-side Security Limitations)
- **إدارة الجلسات والأدوار**: يعتمد تحديد صلاحيات المستخدمين والشركة (`companyId`, `branchId`, `role`) في الوقت الحالي على السياق المحلي للتطبيق (`TenantContext`) والبيانات المخزنة في `localStorage` كذاكرة مؤقتة لسهولة التشغيل.
- **الحماية الجسدية للمتصفح**: يجب توخي الحذر عند إعطاء الكاشير وصولاً لأدوات التطوير (DevTools) لتفادي التلاعب بالصلاحيات المحلية.

## 2. قيود قاعدة البيانات والمزامنة (Database & Sync Limitations)
- **النمط الأوفلاين (Offline Queue)**: عند انقطاع الإنترنت، تحفظ الفواتير في `localStorage` باسم `pending_offline_sales`. تمتلك عملية المزامنة معرفات فريدة (`OFFLINE-timestamp-rand`) لتفادي التكرار عند المزامنة، لكن في حالة كنس الـ Cache يدوياً قبل المزامنة قد تفقد الفواتير المعلقة.
- **عزل الشركات (Multi-Tenancy)**: جميع استعلامات Firestore محمية بقواعد عزل على مستوى الشركة (`companyId`). يجب التأكد دائماً من تسجيل الدخول بحساب مربوط بشركة صحيحة.

## 3. قيود الطباعة الحرارية (Thermal Printer Limitations)
- **التوافق**: يعتمد إرسال أمر الطباعة على بروتوكولات المتصفح المباشرة (`window.print()`) ووسائط ESC/POS عبر المنفذ التسلسلي للمتصفح (Web Serial API) عند توافرها.
- **الطابعات الموصى بها**: طابعات الفواتير الحرارية (80mm أو 58mm) الداعمة لـ USB / ESC/POS.

## 4. قيود الترخيص المالي والتشغيلي (Licensing & Device Locking)
- **بصمة الجهاز (Machine ID)**: يعتمد تحديد ترخيص الجهاز على معرف متصفح محلي ومفتاح التفعيل المبرمج. قد يطلب النظام إعادة التفعيل إذا تم حذف `localStorage` بالكامل.

## 5. قيود النسخ الاحتياطي (Backup & Recovery)
- **التصدير المباشر**: يوفر النظام أداة تصدير JSON كاملة لقاعدة البيانات من شاشة الإعدادات. يُنصح بأخذ نسخة احتياطية يومية يدويًا لحين تفعيل النسخ الاحتياطي التلقائي المجدول على السحابة.

## 6. الديون الفنية والحلول المستقبلية (Technical Debt)
- توحيد تسجيل الدخول عبر Firebase Auth المباشر بدلاً من كلمة السر الرمزية (PIN).
- أتمتة النسخ الاحتياطي السحابي اليومي (Automated Firestore Backups).
